<div class="container">
  <br>
</div>
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-offset-2 col-md-8">
      <div id="calendar-list"></div>
    </div>
  </div>
</div>
<div id="events-path" class="hidden"><%= teaching_set_events_path(@teaching_set) %></div>
<!-- <div class="container">
  <h1>Lessons for <%= @teaching_set.year_group %>, <%= @teaching_set.subject %></h1>
  <ul>
    <% @lessons.each do |lesson| %>
      <%= link_to teaching_set_lesson_path(lesson.teaching_set_id, lesson.id) do %>
      <li><%= lesson.start_time.strftime('%A, %d %b %Y %l:%M %p') %></li>
      <% end %>
    <% end %>
  </ul>
</div> -->
