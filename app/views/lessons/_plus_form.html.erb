<%= simple_form_for(score, remote: true) do |f| %>
  <input name="score[increment]", type="hidden", value="<%= "#{score.value + 1}" %>">
  <%= button_tag type: "submit", class: "btn btn-card" do %>
    <i class="fas fa-plus"></i>
  <% end %>
<% end %>
